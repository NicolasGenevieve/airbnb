import { View, Text } from "react-native";
import { SafeAreaView } from "react-native";

const MapPage = () => {
  return (
    <SafeAreaView>
      <Text>Map Page</Text>
    </SafeAreaView>
  );
};

export default MapPage;
